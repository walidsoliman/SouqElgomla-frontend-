import { DefaultBackgroundDirective } from './default-background.directive';

describe('DefaultBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new DefaultBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});

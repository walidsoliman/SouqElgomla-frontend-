/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { InnercardshadowDirective } from './innercardshadow.directive';

describe('Directive: Innercardshadow', () => {
  it('should create an instance', () => {
    const directive = new InnercardshadowDirective();
    expect(directive).toBeTruthy();
  });
});
